<% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this obj from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
<h2>User info</h2>
<tr><%= image_tag user.get_profile_image(90, 90) %></tr>
      <table class='table'>
  <tbody>
  <tr>
	  <th>name</th>
	  <th><%= user.name %></th>
  </tr>
  <tr>
	  <th>introduction</th>
	  <th><%=user.introduction %></th>
  </tr>
  </tbody>
　　　</table>


<div class='row'>
  <%= link_to edit_user_path(user.id), class: "btn btn-outline-secondary btn-block edit_user_4196" do %>
  <i class="fas fa-user-cog"></i>
  <% end %>
  <%#<a class="btn btn-outline-secondary btn-block edit_user_4196" href="/users/4196/edit"></a%>
</div>

<h1>New book</h1>

<%#= form_with model: Book.new do |f| %>
<%= form_with model: book_new do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Opinion</h4>
  <%= f.text_area :body %>

  <%= f.submit 'Create Book' %>
<% end %>